# Serenity — AI Student Wellness Companion

**Serenity** is a Generative AI-powered Mental Wellness Companion designed specifically for aspirants of high-stakes competitive exams (JEE, NEET, UPSC, GATE, CAT, CUET). By combining structured logs with multi-model Gemini API workflows, Serenity helps students detect emotional fatigue, outline backlog triggers, and ground themselves with voice-guided mindfulness routines.

---

## 🎯 Core Features

1. **Daily Reflections & Mood Log**
   - Rich journaling text area combined with a 1-10 emoji-based mood selector, stress slider, sleep, and focus metrics.
   - Saves entries locally in the browser with timestamps.

2. **Multimodal Study-Log Analysis**
   - **Attach study sheets, backlog logs, or mock test results**.
   - Analyzes files and text using **Gemini 3.1 Flash Lite** to extract hidden stress factors, syllabus issues, and parents' expectations.

3. **Serenity Companion Chat**
   - Chat interface with Serenity AI, fully loaded with the student's study profile and recent reflection logs to preserve context.
   - Powered by **Gemini 3.5 Live** to generate personalized, empathetic study adjustments and advice.

4. **🔊 Voice Guided Mindfulness**
   - **4-7-8 Breathing controller** equipped with a live toggle.
   - Connects to the browser's native **Web Speech API (`speechSynthesis`)** to read calming instructions generated by **Gemini 3.5 Live** aloud on phase changes.
   - Includes step-by-step **5-4-3-2-1 Grounding Cards** to de-escalate anxiety loops.

5. **Integrated Pomodoro Focus Timer**
   - A glassmorphic 25-minute study / 5-minute rest timer.
   - Automatically logs completed focus blocks, pre-filling them directly into your daily journal reflection area.

6. **Interactive Dashboard**
   - Mood trend sparklines (Mood vs. Stress) mapped using **Recharts**.
   - Burnout Risk Dial indicating High/Moderate/Low risk percentages.
   - Weekly stress heatmap grids highlighting high-load weekdays.
   - Trigger word clouds mapping recurring anxiety terms.

---

## 🛠 Tech Stack & Specifications

| Layer | Choice | Purpose |
| :--- | :--- | :--- |
| **Framework** | Next.js 16 (App Router) | Server-Side Rendering, API Handlers |
| **Styling** | Vanilla CSS + CSS Variables | Glassmorphism controls, floating background anims |
| **Generative AI** | Google Gemini SDK | `gemini-3.1-flash-lite` & `gemini-3.5-live` |
| **Audio Synthesis** | Web Speech API | Client-side calming audio readouts |
| **Charts** | Recharts | Interactive wellness timelines |
| **Persistence** | localStorage (Safe Wrappers) | Zero database setup, private local logs |

---

## 🚀 Getting Started

### 1. Clone & Install Dependencies
```bash
npm install
```

### 2. Configure Environment Variables
Create a `.env.local` file in the project root:
```env
GEMINI_API_KEY=your_gemini_api_key_here
```
*Note: If left blank or using the default template, the application automatically triggers **Simulation Mode**—generating high-fidelity simulated insights and chatbot dialogue local to the browser so the app remains fully functional without a key.*

### 3. Run Development Server
```bash
npm run dev
```
Open **`http://localhost:3000`** in your browser to view the application.

---

## 🧪 Verification & Testing

Verify database calculations, analytics aggregates, and simulated response structures using the custom TypeScript test harness:

```bash
npx tsx scratch/test-harness.ts
```

---

## 🌐 Deployment to Vercel

1. Install the Vercel CLI: `npm install -g vercel`
2. Run `vercel` from the root directory to link the project.
3. Configure your environment variable: `vercel env add GEMINI_API_KEY`
4. Deploy to production: `vercel --prod`
